# Configuration Directory - Deny HTTP Access to All Files
# Prevent access to config files and sensitive data from web

# Block all HTTP access to config files
<FilesMatch "\.(php|json)$">
    Deny from all
</FilesMatch>

# Allow access from localhost only (for development/debugging)
Allow from 127.0.0.1
Allow from ::1
